export const LogoCard = ({ img, name, description }) => {
  return (
    <div className="px-5 py-6 bg-white rounded-lg shadow-md border border-gray-200">
      <div className="flex items-center gap-2">
        <img
          src={img}
          alt={name}
          className="size-6 fill-gray-800 dark:fill-gray-100 text-gray-800 dark:text-gray-100"
        />
        <div className="font-semibold text-base text-gray-800 dark:text-white">
          {name}
        </div>
      </div>
      <div className="text-sm text-gray-500 mt-4">{description}</div>
    </div>
  );
};
