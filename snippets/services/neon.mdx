Neon is a managed, serverless Postgres database.

1. Go to [Neon](https://neon.tech/) and sign up for an account.
2. Create a new project.
3. Create a new database and copy the `POSTGRES_URL` and set the following environment variables:

```bash
DATABASE_URL=<your-postgres-url>
```
