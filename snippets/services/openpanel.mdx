OpenPanel is an open-source product analytics platform.

1. Go to [OpenPanel](https://openpanel.dev/) and sign up for an account.
2. Create a new project.
3. Create a new client, copy the `Client ID` and set the following environment variable:

```bash
OPENPANEL_CLIENT_ID=<client-id>
```
