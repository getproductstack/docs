PostHog is an open-source product analytics platform.

1.  Go to [PostHog](https://posthog.com/) and sign up for an account.
2.  Create a new project.
3.  Create a new client, copy the `Client ID` and set the following environment variable:

```bash
POSTHOG_API_KEY=<api-key>
```
